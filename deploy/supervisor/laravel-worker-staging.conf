; =============================================================================
; Laravel Queue Worker — STAGING (Cloudways)
; =============================================================================
; Driver: Redis (set QUEUE_CONNECTION=redis in .env on the server)
;
; Install:
;   sudo cp laravel-worker-staging.conf /etc/supervisor/conf.d/
;   (or paste contents into a new file there)
;
; Replace placeholders before activating:
;   <STAGING_APP_FOLDER>  Application folder from Cloudways → Access Details
;   <STAGING_APP_USER>    SSH/SFTP username for this application
;   <PHP_BINARY>          e.g. /usr/bin/php8.2  (run: which php8.2)
; =============================================================================

[program:laravel-worker-staging]
process_name=%(program_name)s_%(process_num)02d
command=<PHP_BINARY> /home/master/applications/<STAGING_APP_FOLDER>/public_html/artisan queue:work redis --sleep=3 --tries=3 --max-time=3600 --backoff=5 --timeout=60 --queue=default
directory=/home/master/applications/<STAGING_APP_FOLDER>/public_html
autostart=true
autorestart=true
startsecs=1
startretries=3
stopasgroup=true
killasgroup=true
user=<STAGING_APP_USER>
numprocs=1
redirect_stderr=true
stdout_logfile=/home/master/applications/<STAGING_APP_FOLDER>/public_html/storage/logs/worker.log
stdout_logfile_maxbytes=10MB
stdout_logfile_backups=5
stopwaitsecs=3600
environment=LARAVEL_ENV="staging"
